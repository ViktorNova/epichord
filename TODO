Short list

remaining mouse editing
  //resize notes
  //move notes
  paste notes
  paste blocks

remaining tools
  quantize note on
  quantize note off
  join blocks
  split blocks
  unclone block
  (note off tool)
  (aftertouch tool)
  (portamento tool)

enhance recording
  overwrite block
  layer block
  extend block
  new block

undo and redo

midi files
  import smf
  export smf (partially working now)

jack transport


(track toggler)
(sample name)
