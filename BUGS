arranger generally has drawing problems related to follow playback
zero measures per phrase causes division by zero
occasional bug on switching layers

pop up window toggles need to be reset if window closes

import midi doesnt handle misfit events

zoom all the way in draws wrong in piano roll

end of frame sequencing bug
out of order dispatch bug



